syntax = "proto3";

package animal;

service AnimalService {
    rpc createAnimalData(AnimalCreation) returns (AnimalID) {}
    rpc getAnimalData(AnimalID) returns (AnimalData) {}
    rpc deleteAnimalData(AnimalID) returns (Status) {}
    rpc manipulateAnimalData(AnimalData) returns (Status) {}
}

message Status{
    string status = 1;
    string message = 2;
}

message AnimalID {
    string animalid = 1;
}

message Disease {
    string diseaseid = 1;
    string diseasename = 2;
    string diseasedescription = 3;
}

message AnimalCreation {
    string uid = 1;
    string animalname = 2;
    string animaltype = 3;
    string animalrace = 4;
    double animalheight = 5;
    double animalweight = 6;
    repeated Disease diseases = 7;
}

message AnimalData {
    string animalid = 1;
    string uid = 2;
    string animalname = 3;
    string animaltype = 4;
    string animalrace = 5;
    double animalheight = 6;
    double animalweight = 7;
    repeated Disease diseases = 8;
}